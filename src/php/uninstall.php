<?php

if (defined('WP_UNINSTALL_PLUGIN')) {

    // delete_option($option_name);
    // delete_site_option($option_name);
    // global $wpdb;
    // $wpdb->query("DROP TABLE IF EXISTS {$wpdb->prefix}a_table");
}


// vi: se ts=4 sw=4 et:
